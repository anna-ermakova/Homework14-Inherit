public class Bus extends Transport {
    public Bus(String brand, String model, int yearOfRelease, String country, String colorBody, double maxSpeed) {
        super(brand, model, yearOfRelease, country, colorBody, maxSpeed);
    }
}
